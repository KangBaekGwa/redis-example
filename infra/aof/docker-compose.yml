version: "3.8"

services:
  redis-aof:
    image: redis:7.2-alpine
    container_name: redis-aof-test
    ports:
      - "6379:6379"
    volumes:
      - ./redis-aof.conf:/usr/local/etc/redis/redis.conf
      - ./data-aof:/data
    command: redis-server /usr/local/etc/redis/redis.conf
